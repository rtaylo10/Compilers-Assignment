/* A program to compute the factorial value of an input */
void main(void) {
  int x; void fac; void x; int g[9];
  x = input();
  z = x;
  fac = 1;
  g[fac] = 2;

  while (fac > 2) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);

  return x;
}
