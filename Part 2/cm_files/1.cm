/* gcd has been set to a void return type, but still returns and int */
int gcd(int u, int v) {
    int d;
    d=u/v;
  if (v == 0){
  	return u;
  }
  else return gcd(v, u - d*v);
}
void main(void) {
  int x; int y;
  x = 7;
  y = 18;
  output(gcd(x, y));
}
