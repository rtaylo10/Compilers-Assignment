
int gcd(int u, int v) {
    int d;
    d=u/v;
  if (v == 0){ 
  	int x;
  	int y;
  	return u;
  }
  else return gcd(v, u - d*v);
}

void main(void) {
  int x; int y;
  x = input();
  y = input();
  output(gcd(x, y));
}
