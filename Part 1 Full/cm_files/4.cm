/* A program to compute the factorial value of an input	- a list of randomly generated unicode errors are present in the middle of this program	to test its error checking */
void main(void) {
  asdadasdadsa; int x; int fac;
  x = input();
  fac = 1;

  if(x = 1){badinput}

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}
