/* A program to compute the factorial value of an input - A brace was removed at line 10 to test for errors */
void main(void) {
  int x; int fac;
  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;

  output(fac);
}
